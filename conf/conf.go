package conf

const (
	// USER is username for db
	USER string = "root"
	// PASSWORD is password for db
	PASSWORD string = "foo"
	// DB is name of db
	DB string = "door"
	// HOST is host of db
	HOST string = "localhost"
	// PORT is  port number of db
	PORT string = "3306"
)
